import revertByWorld from '../../code/string/[557]反转字符串中的单词-iii'

test("reverseWords: Let's take LeetCode contest", () => {
  expect(revertByWorld("Let's take LeetCode contest")).toBe(
    "s'teL ekat edoCteeL tsetnoc",
  )
})
